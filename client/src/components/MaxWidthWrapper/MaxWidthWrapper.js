import styled from 'styled-components';

const MaxWidthWrapper = styled.div`
  position: relative;
  width: 100%;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
`;

export default MaxWidthWrapper;
